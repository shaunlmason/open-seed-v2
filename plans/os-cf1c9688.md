# Plan: next — the six lane fragments, and the obligations that make them checkable (os-cf1c9688)

Phase 9 item 1, first of three cards (1b: dispatcher least-capability
and the injection corpus; 1c: the worker loop made executable with
exhaustion parking). This card delivers what III.J's first row asks
for and nothing else:

> Role definitions exist for all six lanes as grants + conventions,
> **composable from ordered fragments, resolved and checked by
> validation**.

Plus the four obligations `docs/next-build-plan.md` item 1 binds onto
every fragment (added by os-68ea0b2d, #180).

## The failure mode this exists to prevent

A role file is prose, and prose is where conformance goes to die. v1
already has four of them under `.seed/agents/`, and nothing checks
that what they tell an agent to run exists, or that the verbs they
name are the ones the boundary accepts. That is survivable in v1,
where a human reads the file. It is not survivable for Seed, whose
promotion criterion 1 is *"a lane runs … entirely through Seed verbs,
orienting from one position-stamped read"* — a property of the LANE,
asserted by whoever wrote the file, checked by nobody.

So the deliverable is not six documents. It is six documents **whose
claims are decidable**, each claim checked against a table that
already exists rather than against a new authority. Every check below
either passes today's tables or fails; none of them introduces a new
source of truth.

## Design decisions (binding for this task)

- **D1 — a lane is a manifest plus an ORDERED fragment list; the
  fragments are the prose.** `next/lanes/<lane>.yaml` declares the
  lane's grants, its four obligations, and `fragments: [...]` — an
  explicit ordered list of files under `next/lanes/fragments/`.
  Resolution is concatenation in that order, and `seed lane show
  <name>` prints the resolved text. Ordering is declared, never
  inferred from filenames or directory order: III.J says *ordered*
  fragments, and a resolution whose order comes from a filesystem
  listing is a resolution that changes when someone renames a file.

  Composability is the point of the split: the one-inbox doctrine,
  the escalation shape and the deliberate-exit rule are written ONCE
  and included by all six lanes, so a change to a shared convention
  cannot drift between them. A convention that appears in six files
  is a convention with six versions.

- **D2 — the four obligations are DECLARED FIELDS, not paragraphs.**
  Each manifest carries:

  | field | obligation |
  | --- | --- |
  | `orients_from` | the single position-stamped read on wake |
  | `acts_through` | the loop verbs the lane uses |
  | `liveness_from` | which of the lane's OWN steps emit observations |
  | `inbox` | the one-inbox declaration |

  Prose can restate them; validation reads the fields. An obligation
  a validator cannot find is an obligation the next fragment omits.

- **D3 — every field is checked against an existing table, and this
  is the whole design.** No check below consults a list this card
  invents:

  | check | authority consulted |
  | --- | --- |
  | `grants` name real capabilities | `internal/keyring`'s capability constants |
  | for each verb the lane declares, its grants INTERSECT the accepted set | `keyring.AcceptedCapabilities` per verb |
  | `acts_through` names real loop verbs | the loop-verb registry (D3a) |
  | `orients_from` names a real read surface with real flags | the `situation` flag set |
  | no fragment file is missing or orphaned | the filesystem, against the manifests |
  | resolution is deterministic | resolving twice, byte-compared |

  The middle row is the one that earns the card. A lane that declares
  `budget settle` and is granted only `observer` is a lane whose loop
  refuses on its first spend, and `keyring.AcceptedCapabilities` already
  knows it. That is Phase 8's principle — one rule set, two consumers —
  applied to role definitions.

  **The relation is INTERSECTION, not containment** (review finding on
  this PR). `AcceptedCapabilities` returns an OR-set: the grant rule
  consumes it through `HasAnyCapability`, so any ONE of the returned
  capabilities admits the verb. Most worker verbs return
  `{claim, operator}`. Requiring a lane to hold every accepted
  capability would therefore hand `operator` to every lane that claims
  or spends — dissolving the capability separation the charter is
  built on, in the name of checking it. The check is: for each verb in
  `acts_through`, `grants ∩ AcceptedCapabilities(verb) ≠ ∅`, and the
  finding names the verb and the alternatives when it is empty.

- **D3a — the loop-verb registry has to EXIST before it can be an
  authority.** The plan's first draft cited "`cmd/seed`'s loop-verb
  table". There is no such table (review finding on this PR, verified):
  the seven acts are `case` arms in `runClaim`, `runSubmission` and
  `runBudget` inside package `main`, which `internal/lane` cannot
  import. Citing it would have left the implementer duplicating seven
  names in the validator — violating this plan's own acceptance
  criterion 4 — or refactoring `loop.go` outside the declared scope.

  So this card extracts the registry: one table in
  `next/internal/loopverb` naming each act, its ledger verb, and the
  derivations it performs, consumed by BOTH `cmd/seed`'s dispatch and
  `internal/lane`'s validator. The extraction is IN SCOPE and is the
  card's own thesis applied to itself: an authority with two consumers
  rather than a list written twice. `cmd/seed` keeps its flag parsing
  and envelope handling; only the name-to-verb mapping moves.

- **D4 — the forbidden heartbeat is checked as a PROPERTY, not a
  spelling, and this card checks only the half a manifest can settle.**
  The build plan forbids "a verb whose only purpose is to report
  liveness", and a check for the string `obs emit` would be a spelling
  rule that the next synonym walks past. The decidable form: every step
  named in `liveness_from` must also appear in `acts_through`, and
  `liveness_from` must be **non-empty** — an empty list would otherwise
  satisfy a subset check vacuously (review finding on this PR). A lane
  wanting a heartbeat has to declare a work verb it does not perform,
  and the capability check above then asks it for the grant.

  This is why `liveness_from` names steps rather than verbs: the
  obligation is that liveness rides the work, and a field that named
  verbs directly could satisfy itself.

  **What this does NOT establish, stated rather than implied.** A
  subset check compares two labels. It cannot show that the named step
  actually emits an observation, because in 1a nothing executes: the
  manifests are data and the loop that would emit is 1c's. So the
  obligation is split at the seam where the evidence changes:

  - **1a (here)** — the declaration is present, non-empty, and every
    step it names is a work step the lane performs. A malformed or
    vacuous declaration fails.
  - **1c** — the executable loop must be drilled to show that running
    the declared steps DOES advance the observation stream keyed to
    the lane's actor and fence, and that the loop reaches no
    liveness-only surface. That is a named obligation on 1c, recorded
    here so the split is deliberate rather than a gap discovered later.

  Belt as well as braces: fragment prose is also swept for an
  instruction to run a bare `seed obs emit`, since a fragment could
  otherwise tell an agent to heartbeat without declaring it. That
  sweep IS a spelling rule and is treated as one — a second line of
  defence that catches the careless case, never the argument for the
  property.

- **D5 — the dispatcher's least-capability posture is stated as an
  ALLOWLIST, not as a list of exclusions.** The first draft checked
  that the dispatcher "holds no capability that authors, verdicts, or
  seals". That check passes a dispatcher granted `operator` (review
  finding on this PR), and `operator` is strictly the most powerful
  capability in the keyring: it authorizes actor grants, halts,
  protocol upgrades, cancellations, plan approvals and merge
  overrides, and a governance root holds it implicitly. A
  least-capability rule that lets the strongest grant through is not a
  least-capability rule.

  A blocklist is the wrong shape for the same reason it is elsewhere:
  it must be extended every time a capability is added, and a
  capability nobody thought to exclude is admitted by default. So the
  dispatcher's manifest declares its permitted grants and validation
  checks the set is **exactly** that allowlist — for v0, `dispatch`
  alone, which is what `contract.returned`'s row accepts and what
  routing needs. Any other capability, `operator` first among them,
  fails by name.

  The hostile-corpus suite that proves its INPUT HANDLING is 1b's;
  this card makes the standing claim checkable, and III.J's second row
  is only half met until 1b lands.

- **D6 — the six lanes are the charter's six, named as the charter
  names them** (§11): dispatcher, planner, implementer, verifier,
  curator, maintenance. Not the seven capability rows: a lane is a
  role, a capability is a grant, and the mapping between them is
  exactly what the manifests record. `curator` holds no ledger-writing
  grant at all in v0 (§12: it proposes everything and approves
  nothing), which the manifest states rather than leaves to be
  noticed.

- **D7 — v1 is untouched.** `.seed/agents/`, `scripts/validate.sh` and
  the v1 role fan-out do not change. Seed's lanes live under
  `next/lanes/`, and their validation is a Go drill plus a `seed lane`
  read verb in `next/cmd/seed`, exactly like every other Seed surface.
  The two role systems coexist because the two systems coexist; the
  build plan's §0 integration points do not include this one.

- **D8 — scope guard.** No lane becomes EXECUTABLE here: no loop
  runs, no agent is spawned, no exhaustion parking is wired (1c). No
  hostile corpus (1b). No new ledger verb, no new projection, no
  admission change, and nothing under `next/internal/` outside the
  new package and its drills.

## Steps

1. `next/lanes/` — six manifests and the fragment set: the shared
   fragments (the one-inbox doctrine, the orienting read, the
   deliberate-exit rule, escalation) and the lane-specific ones.
   Fragment prose states the conventions; the manifest states the
   four declarations and the grants.
2. `next/internal/loopverb` — the registry D3a extracts: the seven
   acts, each with its ledger verb, rewired so `cmd/seed`'s dispatch
   reads it instead of open-coding the names. This lands FIRST, with
   the existing loop drills unchanged as the evidence it is inert.
3. `next/internal/lane` — the manifest type, the resolver (ordered
   concatenation), and `Validate`, which runs every D3 check and
   returns findings naming the manifest, the field and the authority
   that refused. No policy of its own: the capability questions go to
   `internal/keyring`, the verb questions to `internal/loopverb`.
4. `next/cmd/seed/lane.go` — `seed lane list`, `seed lane show
   <name>`, `seed lane validate`, in the envelope, read-only,
   journaling nothing (reads are not admission attempts).
5. Drills (`next/internal/loopverb`, `next/internal/lane`,
   `next/cmd/seed`): resolution is ordered and byte-stable; a missing
   fragment, an orphaned fragment, an unknown capability, a verb whose
   accepted set does not intersect the lane's grants, an EMPTY
   `liveness_from`, a `liveness_from` step absent from `acts_through`,
   a fragment instructing a bare `obs emit`, and a dispatcher granted
   anything outside its allowlist (`operator` explicitly among the
   cases) each fail with their own finding; the six shipped manifests
   validate clean; and the loop verbs still dispatch identically after
   the registry extraction.
6. `next/spec/lanes.md` — the surface: what a lane is, what a fragment
   is, the four declarations and why each is a field rather than
   prose, what 1a's liveness check does and does not establish, and
   the conformance mapping to III.J's first and second rows.
7. `next/docs/progress.md`, `next/docs/decisions.md`, memory; receipt;
   evidence; review. progress.md records 1c's inherited obligation to
   drill that the declared liveness steps really do advance the stream.

## File Scope

- `next/lanes/**` (six manifests, the fragment set)
- `next/internal/loopverb/*.go` and its drills (the D3a extraction)
- `next/internal/lane/*.go` and its drills
- `next/cmd/seed/lane.go`, `next/cmd/seed/lane_cli_test.go`
- `next/cmd/seed/loop.go` (dispatch reads the registry; flag parsing
  and envelope handling stay put)
- `next/cmd/seed/main.go` (the one dispatch line for the new verb)
- `next/spec/lanes.md`
- `next/docs/progress.md`, `next/docs/decisions.md`, `memory/*`
- `receipts/os-cf1c9688.json`

## Acceptance Criteria

1. Six manifests exist under `next/lanes/`, one per charter §11 lane,
   each declaring grants and all four obligation fields, each
   composed from an explicitly ordered fragment list.
2. `seed lane show <name>` resolves fragments in declared order and is
   byte-identical across runs; `seed lane list` names the six.
3. `seed lane validate` is green on the shipped set, and each of the
   six failure shapes in step 4 produces a distinct finding naming the
   manifest, the field, and what refused it.
4. Every capability check consults `internal/keyring`; every verb
   check consults `internal/loopverb`. A grep of `internal/lane` finds
   no hand-written list of capability names or verb names outside the
   manifests themselves, and `internal/loopverb` is consumed by BOTH
   the CLI dispatch and the validator — a second copy of the seven
   names anywhere is a failure of this criterion.
5. A lane whose `acts_through` names a verb whose accepted set does
   not INTERSECT its grants fails validation, named by verb and by the
   alternatives that verb accepts. A lane holding one of several
   accepted alternatives passes: the accepted set is an OR-set, and a
   containment check here would force `operator` onto every worker.
6. `liveness_from` is non-empty and every step it names appears in
   `acts_through`; both halves fail separately. The plan states
   explicitly that this settles the declaration's shape and not that
   the step emits, which 1c inherits.
7. The dispatcher's grants equal its declared allowlist exactly.
   `operator` on the dispatcher fails by name, and so does any other
   capability outside the allowlist, including ones added later.
8. No file outside `next/**` changes except `receipts/` and `memory/`;
   `.seed/agents/` and `scripts/validate.sh` are untouched.
9. `make check` green, coverage gate ≥90% held.

## Validation Commands

```sh
cd next && gofmt -l . && go vet ./... && go build ./...
cd next && go test ./internal/lane/ ./cmd/seed/ -count=1
cd next && go test ./internal/loopverb/ -count=1
cd next && go run ./cmd/seed lane validate --lanes ./lanes
make check
```

## Expected diff shape

Six manifests and roughly a dozen fragments (prose), two new internal
packages (the loop-verb registry and the lane resolver/validator), one
CLI verb, one spec file, and the drills. The registry extraction is
the only edit to existing code, and it moves names rather than
behavior: the loop drills pass unchanged, which is the evidence it is
inert. Roughly +800/-40 lines, all under `next/**` plus the
work-product files.

## Open questions deliberately settled here

- **Why not reuse `.seed/agents/`?** Because Seed work never modifies
  v1 surfaces except the integration points the build plan names, and
  this is not one of them. The two systems coexist until promotion,
  and a shared role directory would make the v1 fan-out and Seed's
  validator fight over one file format.
- **Why manifests in YAML and fragments in Markdown?** The
  declarations are data a validator reads; the conventions are prose
  an agent reads. Putting the declarations in frontmatter would work
  and is how v1 does it, but it makes a fragment both a document and
  a record, and the ordered-composition rule then has to say which
  fragment's frontmatter wins. Separating them means a fragment is
  only ever prose and the merge question never arises.
- **Why no `seed lane resolve --write`?** A resolved role is not a
  projection: it derives from checked-in files rather than from the
  ledger, so it has no position to stamp and no rebuild semantics. It
  is computed on demand and printed. Writing it would create a second
  copy that can go stale, which is the exact failure the ordered
  fragment list exists to prevent.
